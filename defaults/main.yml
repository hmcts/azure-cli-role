---
azure_cli_role_yum_packages:
  - gcc
  - libffi-devel
  - python-devel
  - openssl-devel
  - nodejs

azure_cli_role_npm_packages:
  - azure-cli

# override with contents of the .azure/credentials file e.g. from vault
# azure_cli_role_creds_file_content:

azure_cli_role_creds_user: "{{ ansible_user_id }}"

npm_proxy_url: http://reformmgmtproxyout.reform.hmcts.net:8080
